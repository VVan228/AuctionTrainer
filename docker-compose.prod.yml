version: '2' 
services: 
  # client:
  #   container_name: auc-client
  #   build:
  #     context: ./client
  #     args:
  #       - SERVER_URL=http://localhost:8481/
  #   ports:
  #     - "80:80"
  db:
    container_name: auc-db
    build:
      context: ./db
    environment:
      MYSQL_ROOT_PASSWORD: 2
      MYSQL_DATABASE: auction_demo_scheduler
  server:
    container_name: auc-server
    build:
      context: ./server
      args:
        - DATASOURCE_URL=jdbc:mysql://localhost:3306/auction_demo
        - DATASOURCE_PASSWORD=2
        - QRTZ_DATASOURCE_URL=jdbc:mysql://localhost:3306/auction_demo_scheduler
    depends_on:
      db:
        condition: service_healthy
